# 本地参考源码

这些仓库只作为实现参考，不复制或嵌入运行时。

- three.js: `E:\langchain\three.js`
  - 重点参考 `src`、`examples/jsm`、`manual`。
- LangGraph JS: `E:\langchain\langgraphjs`
  - 重点参考 `libs/langgraph`、`examples/multi_agent`、`examples/ui-react`。
- LangChain JS: `E:\langchain\langchainjs`
  - 重点参考 `libs/langchain`、`libs/langchain-core`、`libs/langchain-mcp-adapters`。
- Codex: `E:\langchain\codex`
  - 只借鉴上下文压缩、权限、补丁审批和隔离思路；当前项目不嵌入 Codex CLI。
