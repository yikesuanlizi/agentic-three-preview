# 性能与安全技能

用于检查 Agent 生成代码是否适合浏览器实时预览。

- 不允许网络请求、文件读取、Worker、Service Worker、`eval`、`new Function` 或脚本注入。
- 几何体分段数要适中，避免一次性创建过多对象。
- 不要在动画循环中创建新对象；需要复用 `Vector3`、材质、几何体。
- 预览代码必须是自包含的，错误应能通过 Sandpack console 暴露。
- 如果用户请求危险能力，改为生成等效的本地程序化视觉效果。
